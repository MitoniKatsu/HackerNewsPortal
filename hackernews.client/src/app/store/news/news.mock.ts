import { NewsStateModel } from './news.state';

export const mockNewsState: NewsStateModel = {
  theme: 'dark',
  loadingHome: false,
  loadingSearch: false,
  latestNews: null,
  rankedNews: null,
};
